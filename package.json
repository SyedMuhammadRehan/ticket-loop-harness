{
  "name": "ticket-loop-harness",
  "version": "0.9.0",
  "private": true,
  "description": "Loop-engineering harness for Claude Code — dev harness for the ticket-loop plugin (the plugin itself is zero-dependency).",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node tests/run.js"
  }
}
